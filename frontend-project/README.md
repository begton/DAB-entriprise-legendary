# HRMS Frontend

This is a minimal Vite + React + Tailwind frontend.

Install and run:

```bash
cd frontende-project
npm install
npm run dev
```

Open the app (usually `http://localhost:5173`). The frontend expects the backend at `http://localhost:4000`.
